name: CI

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop, staging, production]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Replace this placeholder with your stack's lint/test steps, or call the
      # shared reusable workflows in Internetworkexpert/reusable-actions.
      - name: Lint and test
        run: echo "TODO: add lint and test steps for this repository."
